# Frontend Configuration
# Leave blank or omit to use the default /api proxy (recommended)
# VITE_API_URL=http://localhost:3000/api

# The app works without the API server — it will use built-in mock data.
# Connect to the API server for production use with real data.
#
# Admin Dashboard:
#   Default credentials: admin@pricewise.ng / pricewise2025
#   Change password immediately after first login!
#
# See server/.env.example for API server configuration
# See scraper/.env.example for scraper configuration
